startStory2();

function startStory2() {
  let preDeathParagraphs = document.getElementById(`2-d`).children;

  for (let preDeathParagraph of preDeathParagraphs) {
    console.log(preDeathParagraph);
  }
}
